# Python
.venv/
.venv-workstation/
__pycache__/
*.pyc
*.egg-info/
dist/
build/

# CMake build tree and the agent's own run/provenance state.
internal/benchmark_fixture/cpp_project/build/
.local-agent/
.local-agent-build-ok
.local-agent-configured-profile

# Benchmark reports are local evidence unless explicitly attached elsewhere.
endpoint-benchmark*.json

# Personal chat personas stay local by default. Neutral and Aiden are explicit examples.
internal/personas/*.toml
!internal/personas/neutral.toml
!internal/personas/aiden.toml

# Model weights are never committed.
models/
*.gguf

# PACKAGE.json is generated release provenance.
PACKAGE.json
