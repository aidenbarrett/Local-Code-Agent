# Provenance notes

This directory records decisions about what belongs to Local Code Agent's measured source identity.

- `session-contract-source-identity.md` explains why normative runtime Session Hub JSON schemas are included in `source_sha256` even though they are stored under `internal/docs/`.
